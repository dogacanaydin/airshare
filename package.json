{
  "name": "airshare",
  "version": "1.0.0",
  "_versionReminder": "⚠️ When updating app version: 1) Update version comment in public/app.js (Line 3), 2) Update VERSION in public/sw.js (Line 3), 3) Ensure both match to avoid PWA cache issues",
  "description": "WebRTC-based file sharing like AirDrop",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.1",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
